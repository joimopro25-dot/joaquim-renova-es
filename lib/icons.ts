import { Hammer, Home, Bath, PaintBucket, TreePine, Ruler, Wrench, Sparkles, LayoutGrid, type LucideIcon } from 'lucide-react';

export const ICONES: Record<string, LucideIcon> = {
  Hammer, Home, Bath, PaintBucket, TreePine, Ruler, Wrench, Sparkles, LayoutGrid,
};

export const NOMES_ICONES = Object.keys(ICONES);
